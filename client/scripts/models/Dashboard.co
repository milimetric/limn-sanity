define ['Storage'], (Storage) ->
    class Dashboard
        (path) ->
            json = Storage.load path
            $.extend(true, @, ko.mapping.fromJSON json)
        
        graphs: ko.observableArray()
